import { extendTheme } from '@nextui-org/react';

export const theme = extendTheme({
  theme: {
    colors: {
      primary: {
        DEFAULT: '#6366f1',
      },
    },
    radii: {
      md: '0.75rem',
    },
  },
});
